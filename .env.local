# Local environment configuration for grants tracking application

# Enable local DynamoDB (IndexedDB) instead of mock data
REACT_APP_USE_LOCAL_DYNAMO=true

# Enable data migration from mock data to IndexedDB
REACT_APP_ENABLE_MIGRATION=true

# Enable debug logging
REACT_APP_DEBUG_LOGGING=true

# Development settings
NODE_ENV=development
