// Simplified database initialization for build compatibility
export const initializeDatabase = async (): Promise<void> => {
  console.log('Database initialization placeholder');
};

// Re-export types
export * from './schema';
